# 1, Name, Age, and Annual Income

## Problem Statement
Write a program that declares the following:
- A `String` variable named `name`
- An `int` variable named `age`
- A `double` variable named `annualPay`

Store your age, name, and desired annual income as literals in these variables. 
The program should display these values on the screen in a manner similar to the following:

My name is Joe Mahoney, my age is 26 and I hope to earn $100000.0 per year.

## Solution Overview
This program demonstrates basic variable declaration and initialization in Java using 
three primitive/reference types (`String`, `int`, `double`), then uses string concatenation 
to display them via `System.out.println()`.

## Files
- `Name.java` — Source file containing the `Name` class with the `main` method.

## How to Compile & Run
```bash
# Compile
javac Name.java

# Run
java Name
```

## Sample Output

-------------- Name, Age, and Annual Income --------------
My name is Yasin Zahir, my age is 28 and I hope to earn $200000.0 per year.
-------------- Name, Age, and Annual Income --------------

## Concepts Practiced
- Variable declaration and initialization
- Primitive data types (`int`, `double`)
- Reference types (`String`)
- String concatenation with the `+` operator
- Console output with `System.out.println()`

## Disclaimer
This README was generated with the assistance of AI.
All code solutions are written by me, Yasin Zahir.